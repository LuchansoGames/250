var Score = function(userimage, username, url, score, position) {
  var sprite = game.add.sprite(0, 0, 'score-background.png');
  var userImage = game.add.sprite(0, 0, userimage);
  var userName = game.add.text();
  var score = game.add.text();
}